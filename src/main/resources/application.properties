#Aquí está la configuración por defecto del servidor:

server.port=8081
#¿Por qué servlet?
server.servlet.context-path=/api/v1 

#At the end of the next line should be the fucking DB
spring.datasource.url=jdbc:mysql://localhost:3306/theorder		
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.initialization-mode=always

#Esta ayuda a mostrar el sql que está lanzando
spring.jpa.show-sql=true
#Sirve para crear las tablas en caso de que no existan
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

logging.level.root: WARN
